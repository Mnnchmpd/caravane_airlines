
<div class="banner" style="background-image: linear-gradient(rgba(0,0,0,0.4),rgba(0,0,0,0.4)), url(https://images.unsplash.com/photo-1514899307441-bee613d8e6e7?ixlib=rb-1.2.1&ixid=eyJhcHBfaWQiOjEyMDd9&auto=format&fit=crop&w=1350&q=80;")>
  <div class="container-a">
    <h1> Welcome to Caravane Airlines </h1>
    <p>Book <strong>YOUR </strong> dream van  <br><em>Rent your van </em><br> </p>
  </div>

  <%= simple_form_for :search, url: root_path, method: :get do |f| %>
    <div class="search-form-control form-group">
      <input class="form-control string required" type="text" name="search[query]" id="search_query" />
      <button name="button" type="submit" class="btn btn-dark">
        <i class="fas fa-search"></i> Search
      </button>

      <% end %>
    </div>
</div>
  <i><h1 class="my-5 text-center new-title" > What is your next travel ? </h1></i>
  <div class="container">


      <div class="d-flex" style="margin-top: 0px">
        <div class="cards">
                <% @products.each do |product| %>
                  <div class="card-trip">

                    <div class="card-category" role="img">
                      <%= cl_image_tag product.photo, height: 250, width: 250, crop: :fill %>
                    </div>


                    <div class="card-trip-infos">

                      <div>
                        <h2><%= "#{product.name}"%></h2>
                        <p><%= "#{product.description}" %></p>
                      </div>

                        <h5><%= "#{product.price}€" %> </h5>

                    </div>


                    <%= link_to "", product_path(product) %>
                    </div>
                    <% end %>
                  </div>
                </div>

            <div id="map" data-markers="<%= @markers.to_json %>"data-mapbox-api-key="<%= ENV['MAPBOX_API_KEY'] %>"></div>

        </div>
    </div>


