<div class="container">
  <div class="row">
    <div class="col-xs-12">
      <h1><%= @product.name %></h1>
      <%= link_to "Back to all Caravan", root_path %>
    </div>
  </div>
  <div class="row">
    <div class="col-sm-12 col-md-6">
      <h3></h3>
      <table class="table table-bordered">
        <tbody>
          <thead>
            <th>Price</th>
            <th>Description</th>
            <th>Price</th>
            <th>Address</th>
          </thead>
            <tr>
              <td><%= @product.name %></td>
              <td><%= @product.description %></td>
              <td><%= @product.price %>€</td>
              <td><%= @product.address %></td>

              </td>
            </tr>

        </tbody>
      </table>
       <%= cl_image_tag @product.photo, height: 300, width: 400, crop: :fill %>
    </div>
  </div>
</div>
