<div class="container">
  <div class="row justify-content-center">
    <div class="col-md-8">
      <h1 class="text-center my-5">Add a new Booking</h1>

        <%= simple_form_for @booking do |f| %>
        <%= f.input :date %>
        <%= f.button :submit, class: 'btn btn-primary' %>
      <% end %>
    </div>
  </div>
</div>
